#!/usr/bin/env python3
"""Runs wallframe straight from a clone of the repository, without installing it.

Same as `python -m wallframe` with src/ on the path. It also works through a
symlink (e.g. ~/.local/bin/wallframe), because the path is resolved to the
real file before looking for src/.
"""

import os
import runpy
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "src"))
runpy.run_module("wallframe", run_name="__main__", alter_sys=True)
